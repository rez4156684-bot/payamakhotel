# راهنمای استفاده با افزونه پیامک حرفه ای ووکامرس

## 📌 اطلاعات مهم سایت شما

بر اساس بررسی سایت qeshmhotel.com، ساختار زیر تشخیص داده شد:

```
افزونه‌های فعال:
✅ JetBooking (نسخه 2.8.0)
✅ JetABAF (Apartment Booking and Filters)
✅ JetBooking Title & SMS Helper (نسخه 1.2.0)
✅ پیامک حرفه ای ووکامرس
```

---

## 🚀 نحوه نصب و راه‌اندازی

### گام 1: نصب افزونه JetBooking SMS Integration

1. فایل `jetbooking-sms-integration.php` را در مسیر زیر قرار دهید:
   ```
   wp-content/plugins/jetbooking-sms-integration/
   ```

2. وارد داشبورد وردپرس شوید
3. به **افزونه‌ها** بروید
4. افزونه **JetBooking SMS Integration** را فعال کنید

---

### گام 2: تنظیم افزونه پیامک حرفه ای ووکامرس

#### یافتن تنظیمات:

```
داشبورد وردپرس
→ ووکامرس فارسی
→ تنظیمات پیامک
```

یا:

```
داشبورد وردپرس
→ تنظیمات
→ پیامک حرفه ای
```

---

### گام 3: ویرایش الگوی پیامک

در قسمت **الگوی پیامک تایید سفارش** (یا هر الگوی دیگر):

#### **مثال 1: استفاده از {booking_info}**

```
مشتری گرامی {billing_first_name}

رزرو شما ثبت شد:

{booking_info}

شماره سفارش: {order_number}
مبلغ: {order_total}

با تشکر از انتخاب شما
{site_title}
```

**خروجی:**
```
مشتری گرامی علی

رزرو شما ثبت شد:

هتل: هتل قشم
اتاق: سوئیت دو تخته
ورود: 1403/09/01
خروج: 1403/09/05
تعداد شب: 4

شماره سفارش: 1234
مبلغ: 5,000,000 تومان

با تشکر از انتخاب شما
qeshmhotel.com
```

---

#### **مثال 2: استفاده از متغیرهای جداگانه**

```
{billing_first_name} عزیز

رزرو شما تایید شد ✅

🏨 {hotel_name}
🛏️ {room_name}
📅 {checkin_date} تا {checkout_date}
🌙 {nights} شب

💰 {order_total}
📱 {order_number}

منتظر دیدار شما هستیم
```

---

## 📝 متغیرهای موجود

| متغیر | توضیحات | مثال |
|-------|---------|------|
| `{hotel_name}` | نام هتل | هتل قشم |
| `{room_name}` | نام اتاق | سوئیت دو تخته |
| `{checkin_date}` | تاریخ ورود | 1403/09/01 |
| `{checkout_date}` | تاریخ خروج | 1403/09/05 |
| `{nights}` | تعداد شب | 4 |
| `{booking_info}` | اطلاعات کامل | همه موارد بالا |

---

## ⚙️ تنظیمات پیشرفته

### گزینه 1: استفاده همزمان با JetBooking Title & SMS Helper

اگر می‌خواهید از هر دو افزونه استفاده کنید:

1. **JetBooking Title & SMS Helper**: برای تغییر عنوان آیتم سفارش
2. **JetBooking SMS Integration**: برای اضافه کردن متغیرها به پیامک

هر دو می‌توانند همزمان فعال باشند.

---

### گزینه 2: استفاده فقط از JetBooking SMS Integration

اگر می‌خواهید فقط از افزونه من استفاده کنید:

1. **JetBooking Title & SMS Helper** را غیرفعال کنید
2. فقط **JetBooking SMS Integration** را فعال نگه دارید
3. از متغیرها در الگوی پیامک استفاده کنید

---

## 🔧 رفع مشکلات

### مشکل 1: متغیرها در پیامک نمایش داده نمی‌شوند

**راه حل:**

1. مطمئن شوید افزونه **JetBooking SMS Integration** فعال است
2. یک سفارش **جدید** ثبت کنید (سفارش‌های قدیمی اطلاعات را ندارند)
3. از `{booking_info}` استفاده کنید، نه متغیرهای جداگانه

---

### مشکل 2: تاریخ‌ها به درستی نمایش داده نمی‌شوند

**راه حل:**

- مطمئن شوید افزونه **پارسی‌دیت** فعال است
- افزونه به صورت خودکار از تقویم شمسی استفاده می‌کند

---

### مشکل 3: نام هتل خالی است

**راه حل:**

1. در JetBooking، مطمئن شوید واحد (Unit) تعیین شده است
2. اگر واحد خالی است، نام سایت (`qeshmhotel.com`) نمایش داده می‌شود

---

## 🔍 حالت Debug

برای دیدن دقیق اطلاعات استخراج شده:

### فعال کردن Debug:

در فایل افزونه، **خط 28** را تغییر دهید:

```php
// قبل:
define('JETBOOKING_SMS_DEBUG', false);

// بعد:
define('JETBOOKING_SMS_DEBUG', true);
```

### مشاهده لاگ‌ها:

```
wp-content/debug.log
```

سفارش تستی ثبت کنید و فایل لاگ را بررسی کنید.

---

## 📱 تنظیمات پیشنهادی برای "پیامک حرفه ای ووکامرس"

### الگوی پیامک تایید سفارش (برای مشتری):

```
{billing_first_name} عزیز

رزرو شما در {site_title} ثبت شد

{booking_info}

شماره پیگیری: {order_number}
مبلغ قابل پرداخت: {order_total}

لینک پرداخت:
{order_pay_url}

با تشکر
```

---

### الگوی پیامک پرداخت موفق (برای مشتری):

```
{billing_first_name} عزیز

پرداخت شما با موفقیت انجام شد ✅

{booking_info}

شماره سفارش: {order_number}
مبلغ پرداختی: {order_total}

منتظر دیدار شما هستیم
{site_title}
{site_phone}
```

---

### الگوی پیامک سفارش جدید (برای مدیر):

```
🔔 سفارش جدید در {site_title}

مشتری: {billing_first_name} {billing_last_name}
موبایل: {billing_phone}

{booking_info}

مبلغ: {order_total}
وضعیت: {order_status}

مشاهده سفارش:
{admin_order_url}
```

---

## ✅ چک‌لیست نهایی

قبل از استفاده، این موارد را بررسی کنید:

- [ ] JetBooking فعال است
- [ ] JetBooking SMS Integration نسخه 1.3.0 یا بالاتر نصب و فعال است
- [ ] افزونه پیامک حرفه ای ووکامرس فعال است
- [ ] الگوی پیامک ویرایش شده و متغیرها اضافه شده‌اند
- [ ] پارسی‌دیت فعال است (برای تاریخ شمسی)
- [ ] یک سفارش تستی ثبت کرده‌اید
- [ ] پیامک را دریافت کرده و محتوا را بررسی کرده‌اید

---

## 📞 پشتیبانی

اگر با مشکلی مواجه شدید:

1. حالت Debug را فعال کنید
2. سفارش تستی ثبت کنید
3. فایل `debug.log` را بررسی کنید
4. اسکرین‌شات از تنظیمات پیامک بگیرید
5. متن پیامک دریافتی را کپی کنید

---

## 🎯 نکته نهایی

**مهم:** اگر پیامک شامل "رزرو" است و متغیرها کار نمی‌کنند:

1. مطمئن شوید سفارش **جدید** است (بعد از نصب افزونه)
2. از متغیر `{booking_info}` استفاده کنید
3. افزونه به صورت خودکار کلمه "رزرو" را با اطلاعات کامل جایگزین می‌کند

---

**موفق باشید!** 🚀
