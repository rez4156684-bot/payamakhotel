# نسخه 1.4.0 - راهنمای استفاده از پنل مدیریت پیشرفته

## 🎉 ویژگی‌های جدید نسخه 1.4.0

این نسخه شامل یک **پنل مدیریت حرفه‌ای** با ابزارهای کامل دیباگ و تست است.

---

## 📍 نحوه دسترسی به پنل مدیریت

بعد از نصب و فعال کردن افزونه:

```
داشبورد وردپرس
→ ووکامرس
→ JetBooking SMS
```

---

## 📊 تب‌های موجود

### 1️⃣ **داشبورد**
- نمایش وضعیت افزونه‌های مورد نیاز (ووکامرس، JetBooking)
- آمار کلی سفارشات
- جدول متغیرهای موجود
- راهنمای شروع سریع

**چه زمانی استفاده کنیم؟**
- برای بررسی اولیه وضعیت سیستم
- برای دیدن لیست متغیرهای موجود

---

### 2️⃣ **سفارشات**
- لیست آخرین سفارشات با اطلاعات رزرو
- نمایش نام هتل، اتاق، تاریخ ورود و خروج
- دکمه مشاهده جزئیات برای هر سفارش

**چه زمانی استفاده کنیم؟**
- برای دیدن سریع اینکه کدام سفارشات اطلاعات رزرو دارند
- برای پیدا کردن سفارش مشکل‌دار

---

### 3️⃣ **فیلترها**
- لیست فیلترهای SMS که افزونه به آن‌ها متصل شده
- وضعیت هر فیلتر (فعال/غیرفعال)
- لیست تمام فیلترهای SMS در سیستم

**چه زمانی استفاده کنیم؟**
- برای تشخیص اینکه افزونه پیامکی شما از چه فیلتری استفاده می‌کند
- برای بررسی اینکه افزونه ما به فیلتر درست متصل شده یا نه

**مثال:**
اگر افزونه پیامکی شما فیلتر `custom_sms_filter` استفاده می‌کند و در لیست نیست، باید آن را به کد اضافه کنیم.

---

### 4️⃣ **تست** ⭐ مهم‌ترین تب!
این تب برای رفع مشکل استفاده می‌شود.

**ویژگی‌ها:**
- نمایش تمام متادیتای سفارش
- نمایش متادیتای آیتم‌های سفارش
- نمایش اطلاعات رزرو استخراج شده
- تست پردازش متن پیامک
- دکمه کپی برای هر بخش

**نحوه استفاده:**

1. **انتخاب سفارش**
   - یک سفارش را از لیست انتخاب کنید
   - روی "تست و بررسی" کلیک کنید

2. **بررسی متادیتای سفارش**
   - تمام کلیدها و مقادیر را ببینید
   - روی "📋 کپی متادیتا" کلیک کنید
   - برای من ارسال کنید

3. **بررسی آیتم‌های سفارش**
   - متادیتای هر آیتم را ببینید
   - کلیدهایی مثل `apartment_unit`، `check_in_date` را پیدا کنید
   - روی "📋 کپی" کلیک کنید

4. **اطلاعات رزرو استخراج شده**
   - ببینید افزونه چه اطلاعاتی را استخراج کرده
   - اگر خالی است، مشکل در استخراج است
   - روی "📋 کپی اطلاعات رزرو" کلیک کنید

5. **تست متن پیامک**
   - ببینید متن پیامک چطور پردازش می‌شود
   - اگر {booking_info} جایگزین نشده، مشکل در این بخش است
   - روی "📋 کپی پیامک" کلیک کنید

---

### 5️⃣ **لاگ‌ها**
- نمایش فایل debug.log
- فیلتر شده: فقط لاگ‌های مرتبط با JetBooking
- تمام لاگ‌ها: 1000 خط آخر

**نحوه استفاده:**

1. **فعال کردن Debug**
   ```php
   // در فایل افزونه، خط 28:
   define('JETBOOKING_SMS_DEBUG', true);
   ```

2. **ثبت سفارش تستی**
   - یک رزرو جدید ثبت کنید

3. **مشاهده لاگ‌ها**
   - به تب لاگ‌ها بروید
   - روی "📋 کپی لاگ‌ها" کلیک کنید
   - برای من ارسال کنید

---

### 6️⃣ **راهنما**
- راهنمای گام به گام استفاده
- نکات مهم
- راهنمای رفع مشکل

---

## 🔍 سناریوی رفع مشکل

### مشکل: {booking_info} کار نمی‌کند

#### گام 1: بررسی وضعیت (تب داشبورد)
- [ ] ووکامرس فعال است؟
- [ ] JetBooking فعال است؟

#### گام 2: بررسی فیلترها (تب فیلترها)
- [ ] کدام فیلترهای SMS فعال هستند؟
- [ ] افزونه ما به فیلتر درست متصل شده؟

#### گام 3: تست با سفارش (تب تست)
- [ ] یک سفارش انتخاب کنید
- [ ] متادیتای سفارش را کپی کنید
- [ ] متادیتای آیتم‌ها را کپی کنید
- [ ] اطلاعات رزرو استخراج شده را کپی کنید
- [ ] متن پیامک پردازش شده را کپی کنید

#### گام 4: فعال کردن Debug (تب لاگ‌ها)
- [ ] Debug را فعال کنید
- [ ] سفارش تستی ثبت کنید
- [ ] لاگ‌ها را کپی کنید

#### گام 5: ارسال اطلاعات
همه موارد بالا را برای من ارسال کنید تا مشکل را پیدا کنم!

---

## 📋 چک‌لیست برای ارسال اطلاعات

اگر مشکلی دارید، این موارد را کپی و ارسال کنید:

### ✅ از تب تست:
- [ ] متادیتای سفارش (از بخش "متادیتای سفارش")
- [ ] متادیتای آیتم‌ها (از بخش "آیتم‌های سفارش")
- [ ] اطلاعات رزرو استخراج شده (از بخش "اطلاعات رزرو")
- [ ] متن پیامک پردازش شده (از بخش "تست متن پیامک")

### ✅ از تب فیلترها:
- [ ] لیست فیلترهای SMS فعال (از بخش "تمام فیلترهای SMS")

### ✅ از تب لاگ‌ها:
- [ ] لاگ‌های مرتبط (اگر Debug فعال است)

### ✅ اطلاعات اضافی:
- [ ] نام دقیق افزونه پیامکی شما
- [ ] الگوی پیامکی که استفاده می‌کنید
- [ ] متن پیامک دریافتی (چه چیزی نشان می‌دهد)

---

## 💡 نکات مهم

### ⚠️ سفارش‌های قدیمی
سفارش‌های ثبت شده **قبل از نصب افزونه** اطلاعات رزرو ندارند!

**راه حل:**
- یک سفارش جدید ثبت کنید
- از سفارش‌های بعد از نصب افزونه استفاده کنید

### 🔄 بروزرسانی اطلاعات
اگر افزونه را بروزرسانی کردید:
- پاک کردن کش
- غیرفعال و فعال کردن مجدد افزونه
- ثبت سفارش جدید برای تست

### 🎯 بهترین روش تست
1. حالت Debug را فعال کنید
2. یک سفارش کاملاً جدید ثبت کنید
3. به تب تست بروید
4. تمام اطلاعات را کپی کنید
5. برای پشتیبانی ارسال کنید

---

## 🚀 مثال استفاده عملی

### وضعیت: پیامک فقط "رزرو" نشان می‌دهد

**گام‌های رفع مشکل:**

1. **تب تست** → انتخاب آخرین سفارش
2. بررسی **متادیتای آیتم‌ها**:
   ```
   Key: apartment_unit
   Value: 123

   Key: check_in_date
   Value: 2024-01-01
   ```
3. بررسی **اطلاعات رزرو استخراج شده**:
   ```
   Array
   (
       [hotel_name] => هتل قشم
       [room_name] => سوئیت دو تخته
       [checkin_date] => 1403/09/01
       ...
   )
   ```
4. بررسی **متن پیامک**:
   - اگر اطلاعات نمایش داده می‌شود ← مشکل در فیلتر افزونه پیامکی است
   - اگر اطلاعات نمایش داده نمی‌شود ← مشکل در استخراج است

5. **تب فیلترها** → کپی لیست فیلترها
6. **تب لاگ‌ها** → کپی لاگ‌ها
7. ارسال همه برای پشتیبانی

---

## 🎓 آموزش ویدیویی (مراحل)

### 1. نصب و فعال‌سازی
- آپلود فایل
- فعال کردن
- رفتن به پنل مدیریت

### 2. بررسی اولیه
- تب داشبورد: چک کردن وضعیت
- تب سفارشات: دیدن لیست

### 3. تست و رفع مشکل
- تب تست: انتخاب سفارش
- کپی کردن اطلاعات
- ارسال برای پشتیبانی

---

**موفق باشید!** 🚀

اگر سوالی دارید یا مشکلی دیدید، با استفاده از پنل مدیریت تمام اطلاعات را کپی کرده و برای من ارسال کنید.
