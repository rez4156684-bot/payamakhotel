# 🔗 راهنمای یکپارچه‌سازی ویجیت با سیستم رزرو

## 📋 خلاصه تغییرات

ویجیت Elementor حالا به طور کامل با **افزونه WooCommerce Hotel Reservation System** یکپارچه شده است.

---

## ✨ قابلیت‌های یکپارچه شده

### 1️⃣ اطلاعات هتل
ویجیت اطلاعات زیر را از افزونه Hotel Reservation می‌خواند:

```php
✓ امکانات هتل (_hotel_amenities)
✓ ساعت ورود (_hotel_check_in_time)
✓ ساعت خروج (_hotel_check_out_time)
✓ درجه/ستاره هتل (_hotel_rating)
✓ قوانین هتل (_hotel_rules)
✓ لیست اتاق‌ها (_hotel_rooms)
```

### 2️⃣ توضیحات محصول
از **توضیحات کوتاه** یا **توضیحات کامل** محصول ووکامرس استفاده می‌شود:

```php
Priority:
1. Short Description (توضیحات کوتاه)
2. Full Description (اگر توضیحات کوتاه خالی باشد)
```

### 3️⃣ مخفی کردن المان‌های پیش‌فرض
ویجیت به صورت خودکار این المان‌ها را مخفی می‌کند:

```
✓ گالری محصول پیش‌فرض
✓ عنوان محصول
✓ قیمت محصول
✓ فرم افزودن به سبد
✓ دکمه خرید
✓ متا محصول
✓ Breadcrumb
✓ تب‌های محصول
```

### 4️⃣ سیستم رزرو
بخش رزرو از **افزونه Hotel Reservation** استفاده می‌کند که شامل:

```
✓ تقویم شمسی
✓ انتخاب تاریخ ورود/خروج
✓ انتخاب اتاق
✓ محاسبه قیمت
✓ افزودن به سبد خرید
```

### 5️⃣ پیامک
یکپارچه با **افزونه JetBooking SMS Integration** که قبلاً نصب شده:

```
✓ ارسال اطلاعات رزرو در پیامک
✓ اطلاعات هتل و اتاق
✓ تاریخ ورود و خروج
✓ تعداد شب‌ها
```

---

## 🚀 نصب و راه‌اندازی

### مرحله 1: نصب افزونه Hotel Reservation

ابتدا باید افزونه WooCommerce Hotel Reservation را نصب کنید:

```php
// فایل افزونه را در مسیر زیر قرار دهید:
wp-content/plugins/wc-hotel-reservation/

// یا از کد ارسالی استفاده کنید
```

فعال‌سازی: **افزونه‌ها** → **WooCommerce Hotel Reservation** → فعال

### مرحله 2: پیکربندی محصول هتل

1. **ایجاد محصول**
   ```
   محصولات → افزودن
   نام: "هتل 5 ستاره تهران"
   ```

2. **فعال‌سازی سیستم رزرو**
   ```
   تب "اتاق‌های هتل"
   ✓ فعال‌سازی سیستم رزرو
   ذخیره محصول
   ```

3. **تنظیمات عمومی هتل**
   در متاباکس "تنظیمات عمومی هتل":

   ```
   ساعت ورود: 14:00
   ساعت خروج: 12:00
   درجه: 5 (یا "5 ستاره")
   ```

4. **افزودن امکانات**
   ```
   آیکون: 📶 | متن: وای‌فای رایگان
   آیکون: ❄️ | متن: تهویه مطبوع
   آیکون: 📺 | متن: تلویزیون
   آیکون: 🚿 | متن: حمام اختصاصی
   آیکون: ☕ | متن: چای و قهوه
   آیکون: 🅿️ | متن: پارکینگ رایگان
   ```

5. **افزودن اتاق**
   ```
   نام اتاق: سوئیت دو تخته
   قیمت پایه: 500000 تومان/شب
   ظرفیت: 2 نفر
   توضیحات: اتاق مجهز با تمام امکانات
   ```

6. **قوانین هتل**
   ```
   - ورود از ساعت 14:00 و خروج تا 12:00
   - لغو رایگان تا 24 ساعت قبل از ورود
   - ممنوعیت سیگار در اتاق‌ها
   ```

7. **گالری و توضیحات**
   - تصویر شاخص: عکس اصلی هتل
   - گالری محصول: 3-4 عکس دیگر
   - توضیحات کوتاه: معرفی کوتاه هتل

8. **انتشار**
   - دکمه "انتشار" را بزنید

### مرحله 3: استفاده در Elementor

#### حالت A: صفحه معمولی

```
1. صفحات → افزودن
2. ویرایش با Elementor
3. جستجو: "رزرو هتل (یکپارچه)"
4. کشیدن ویجیت به صفحه
5. انتخاب محصول از لیست
6. تنظیمات:
   ✓ نمایش گالری
   ✓ نمایش توضیحات
   ✓ مخفی کردن المان‌های پیش‌فرض
7. انتشار
```

#### حالت B: قالب Single Product (پیشنهادی)

```
1. Elementor → Theme Builder
2. Add New → Single Product
3. نام: "Hotel Product Template"
4. حذف تمام محتوای پیش‌فرض
5. اضافه کردن ویجیت "رزرو هتل (یکپارچه)"
6. تنظیمات ویجیت:
   - استفاده از محصول جاری: ✓ فعال
   - نمایش گالری: ✓ فعال
   - نمایش توضیحات: ✓ فعال
   - مخفی کردن المان‌های پیش‌فرض: ✓ فعال
7. Display Conditions:
   - Products in Category → هتل‌ها
   یا
   - Include → Products → [محصولات خاص]
8. Publish
```

---

## 🎨 تنظیمات ویجیت

### تب Content (محتوا)

| تنظیم | توضیح | پیش‌فرض |
|------|-------|---------|
| محصول هتل | انتخاب محصول از لیست | - |
| استفاده از محصول جاری | برای Theme Builder | خیر |
| نمایش گالری | نمایش تصاویر محصول | بله |
| نمایش توضیحات | توضیحات ووکامرس | بله |
| مخفی کردن المان‌های پیش‌فرض | مخفی کردن UI ووکامرس | بله |

### تب Style (استایل)

```
استایل کارت:
├── رنگ پس‌زمینه
├── حاشیه (Border)
├── گردی گوشه (Border Radius)
└── سایه (Box Shadow)
```

---

## 📱 نمایش در صفحه

### ساختار صفحه

```
┌─────────────────────────────────┐
│  [گالری تصاویر + درجه هتل]      │
├─────────────────────────────────┤
│  نام هتل                        │
│  ورود: 14:00 | خروج: 12:00      │
├─────────────────────────────────┤
│  توضیحات محصول (از ووکامرس)    │
├─────────────────────────────────┤
│  امکانات هتل (از افزونه)        │
│  📶 وای‌فای | ❄️ تهویه          │
├─────────────────────────────────┤
│  قوانین و مقررات                │
├─────────────────────────────────┤
│  [پیام: برای رزرو پایین بروید]  │
│  اتاق‌های موجود (3 اتاق)        │
│  - سوئیت دو تخته: 500,000 تومان │
└─────────────────────────────────┘

⬇️ (اسکرول پایین)

┌─────────────────────────────────┐
│  [سیستم رزرو از افزونه اصلی]    │
│  🔍 جستجوی اقامت                │
│  📅 تاریخ ورود | 🌙 تعداد شب   │
│  [دکمه جستجو]                   │
├─────────────────────────────────┤
│  [کارت‌های اتاق با تقویم]        │
│  [دکمه رزرو آنلاین]             │
└─────────────────────────────────┘
```

---

## 🔧 عیب‌یابی

### مشکل: محصول در لیست نمایش داده نمی‌شود

**علت**: سیستم رزرو فعال نیست

**راه‌حل**:
```
1. ویرایش محصول
2. تب "اتاق‌های هتل"
3. ✓ فعال‌سازی سیستم رزرو
4. ذخیره
```

### مشکل: المان‌های پیش‌فرض هنوز نمایش داده می‌شوند

**علت**: CSS کش شده است

**راه‌حل**:
```
1. Elementor → Tools → Regenerate CSS
2. پاک کردن کش مرورگر
3. یا CSS سفارشی اضافه کنید:

.product .woocommerce-product-gallery { display: none !important; }
```

### مشکل: امکانات نمایش داده نمی‌شوند

**علت**: امکانات در محصول تنظیم نشده

**راه‌حل**:
```
1. ویرایش محصول
2. متاباکس "تنظیمات عمومی هتل"
3. اضافه کردن امکانات
4. ذخیره
```

### مشکل: تقویم کار نمی‌کند

**علت**: این مربوط به افزونه Hotel Reservation است

**راه‌حل**:
```
بخش رزرو توسط افزونه اصلی مدیریت می‌شود.
برای رفع مشکل تقویم به تنظیمات افزونه مراجعه کنید.
```

---

## 💡 نکات مهم

### 1. دو بخش جدا

```
بخش بالا (ویجیت Elementor):
✓ گالری
✓ توضیحات
✓ امکانات
✓ قوانین

بخش پایین (افزونه Hotel Reservation):
✓ جستجو
✓ تقویم
✓ انتخاب اتاق
✓ رزرو
```

### 2. پیامک خودکار

افزونه **JetBooking SMS Integration** که قبلاً نصب شده، به صورت خودکار:

```
✓ اطلاعات رزرو را ذخیره می‌کند
✓ در پیامک‌ها اضافه می‌کند
✓ متغیرهای زیر را جایگزین می‌کند:
  - {hotel_name}
  - {room_name}
  - {checkin_date}
  - {checkout_date}
  - {nights}
```

### 3. سفارشی‌سازی

برای تغییر ظاهر:

```css
/* در Elementor → Custom CSS */

/* تغییر رنگ اصلی */
.hbw-integrated-card {
    border-color: #your-color !important;
}

/* تغییر فونت */
.hbw-title {
    font-family: 'Your Font', sans-serif;
}
```

---

## 📊 جدول مقایسه

| ویژگی | ویجیت قبلی | ویجیت یکپارچه |
|-------|-----------|---------------|
| منبع داده اتاق‌ها | داخلی | افزونه Hotel Reservation |
| منبع امکانات | ثابت | افزونه Hotel Reservation |
| توضیحات | ثابت | ووکامرس |
| تقویم | داخلی (Flatpickr) | افزونه Hotel Reservation |
| رزرو | داخلی | افزونه Hotel Reservation |
| پیامک | جدا | JetBooking SMS Integration |
| المان‌های پیش‌فرض | نمایش | مخفی |

---

## ✅ چک‌لیست نهایی

- [ ] افزونه Hotel Reservation نصب و فعال شده
- [ ] افزونه JetBooking SMS فعال است
- [ ] محصول هتل ایجاد شده
- [ ] سیستم رزرو فعال شده
- [ ] امکانات، قوانین و ساعات تنظیم شده
- [ ] حداقل یک اتاق اضافه شده
- [ ] گالری و توضیحات اضافه شده
- [ ] ویجیت در Elementor اضافه شده
- [ ] تنظیمات ویجیت انجام شده
- [ ] قالب Single Product ایجاد شده (اختیاری)
- [ ] تست رزرو انجام شده
- [ ] پیامک تست شده

---

## 🎉 تبریک!

سیستم یکپارچه شما آماده است!

اکنون شما یک سیستم کامل دارید که:
- از قابلیت‌های حرفه‌ای Hotel Reservation استفاده می‌کند
- ظاهر زیبا و مدرن دارد
- با سیستم پیامک یکپارچه است
- کاملاً ریسپانسیو است

---

**پشتیبانی**: برای سوال یا مشکل، Issue جدید در GitHub ایجاد کنید.

**نسخه**: 1.1.0 - یکپارچه‌سازی کامل | تاریخ: 2024-12-08
